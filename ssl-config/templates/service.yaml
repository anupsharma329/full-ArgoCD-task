apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssl-config.fullname" . }}
  labels:
    {{- include "ssl-config.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssl-config.selectorLabels" . | nindent 4 }}
